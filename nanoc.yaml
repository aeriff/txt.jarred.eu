base_url: "https://txt.jarred.eu"
enable_output_diff: true
prune:
  auto_prune: true
  exclude: [ '.git', 'CNAME' ]
data_sources:
  - type: filesystem
deploy:
  default:
    kind: git
    branch: gh-pages
